# kind_commands

```bash
# check clusters
kind get clusters

# create cluster
kind create cluster --name kind-local

# create cluster with file
kind create cluster --config kind_local_config.yaml --name kind-local

# delete cluster
kind delete cluster --name kind-local
```
